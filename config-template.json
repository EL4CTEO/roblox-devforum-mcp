{
  "$comment": "Copy the relevant block into your MCP client config.",

  "claude-desktop": {
    "$comment": "Primary method — no clone required. First launch downloads + builds (~30s), then instant.",
    "mcpServers": {
      "roblox-devforum": {
        "command": "npx",
        "args": ["-y", "github:EL4CTEO/roblox-devforum-mcp"]
      }
    }
  },

  "claude-desktop-local": {
    "$comment": "Fallback: clone repo then use local path. Required on Windows if npx times out.",
    "mcpServers": {
      "roblox-devforum": {
        "command": "node",
        "args": ["/path/to/roblox-devforum-mcp/build/index.js"]
      }
    }
  },

  "opencode": {
    "mcp": {
      "roblox-devforum": {
        "type": "local",
        "command": ["npx", "-y", "github:EL4CTEO/roblox-devforum-mcp"]
      }
    }
  },

  "opencode-local": {
    "$comment": "Fallback for Windows or if npx is slow.",
    "mcp": {
      "roblox-devforum": {
        "type": "local",
        "command": ["node", "/path/to/roblox-devforum-mcp/build/index.js"]
      }
    }
  },

  "claude-code": {
    "$comment": "Run in terminal:",
    "$command": "claude mcp add roblox-devforum -- npx -y github:EL4CTEO/roblox-devforum-mcp"
  },

  "cloudflare-worker": {
    "$comment": "Deploy once, use on desktop/web/mobile via claude.ai Connectors.",
    "$setup": [
      "npx wrangler login",
      "npm run worker:secret",
      "npm run worker:deploy"
    ],
    "connector": {
      "name": "Roblox DevForum",
      "url": "https://roblox-devforum-mcp.YOURNAME.workers.dev/mcp",
      "auth": {
        "header": "Authorization",
        "value": "Bearer YOUR_AUTH_TOKEN"
      }
    }
  }
}
