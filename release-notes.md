## What's new

- MCPB bundle (.mcpb): one-click install in Claude Desktop. Settings → Extensions → Install Extension → select the file.
- Creator Hub docs: fixed JSX rendering (readable text instead of raw code)
- Creator Store: graceful fallback when asset type not in catalog
- Forum search: fixed top sort URL
- Forum taxonomy: category_meta fallback
- 45 tests passing, TypeScript strict, 37/40 QA tests passing

## Install

Download `roblox-devforum-5.1.1.mcpb` and install via Claude Desktop → Settings → Extensions.

Or use npx: `npx -y github:EL4CTEO/roblox-devforum-mcp`